# Common tier: installed on every machine.
# One logical name per line. The logical name is the binary that
# `command -v` will look for. Package-manager-specific renames live in
# packages/renames/<pkg-mgr>.txt.

git
gpg
sops
age
just
nvim
tmux
starship
rg
fd
fzf
jq
gh
npm
curl
openssl
bats
claude
gemini

