# Template seed for the personal "desktop" type list
# (~/.config/dj/packages/types/desktop.txt, used when
# `dj system-type desktop` is set).
#
# GUI / Wayland / interactive tooling. Tools not available on a given
# package manager should be SKIP'd in packages/renames/<pkg-mgr>.txt.
# Populate as desktops are set up.
